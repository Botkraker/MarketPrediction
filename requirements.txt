# Pinned to the versions the pipeline was last run with (Python 3.14, Windows).
#
# torch is the CUDA 12.8 build; pip needs the PyTorch index for it:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu128
# On a CPU-only machine drop the "+cu128" suffix -- everything runs, just slower.

# core data stack
numpy==2.5.3
pandas==3.0.6
pyarrow==25.0.1
scipy==1.18.1
scikit-learn==1.9.1
statsmodels==0.15.0
matplotlib==3.11.2
duckdb==1.5.5
openpyxl==3.1.5

# scraping
requests==2.34.2
curl_cffi==0.16.3
lxml==6.1.3

# language id
langdetect==1.0.9

# transformer scorers (FinBERT, OPUS-MT, CamemBERT / XLM-R)
torch==2.11.0+cu128
transformers==5.17.0
tokenizers==0.23.2
sentencepiece==0.2.2
sacremoses==0.2.0

# tooling
pytest==9.1.1
dvc==3.67.1
